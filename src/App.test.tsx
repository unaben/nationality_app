import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

describe("App component", () => {
  test("should renders correctly", () => {
  
  });

  test("should renders heading h1 and content", () => {
    render(<App />);

  });
});
